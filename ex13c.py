import sys

script, first_arg, second_arg = sys.argv

print(f"The script is called {script}.  The arguments are {first_arg} and {second_arg}.")

author = input("What is the author's name? ")

print(f"This was written by {author}.")
